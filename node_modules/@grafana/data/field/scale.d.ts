import { Field, FieldConfig, GrafanaTheme, Threshold } from '../types';
export interface ColorScaleValue {
    percent: number;
    threshold: Threshold;
    color: string;
}
export declare type ScaleCalculator = (value: number) => ColorScaleValue;
export declare function getScaleCalculator(field: Field, theme: GrafanaTheme): ScaleCalculator;
export declare function getFieldConfigWithMinMax(field: Field, local?: boolean): FieldConfig;
