import React from 'react';
import { DataFrame, FieldMatcher } from '@grafana/data';
import { PlotProps } from '../uPlot/types';
import { VizLegendOptions } from '../VizLegend/types';
import { GraphNGLegendEvent } from './types';
export interface XYFieldMatchers {
    x: FieldMatcher;
    y: FieldMatcher;
}
export interface GraphNGProps extends Omit<PlotProps, 'data' | 'config'> {
    data: DataFrame[];
    legend: VizLegendOptions;
    fields?: XYFieldMatchers;
    onLegendClick?: (event: GraphNGLegendEvent) => void;
    onSeriesColorChange?: (label: string, color: string) => void;
}
export declare const FIXED_UNIT = "__fixed";
export declare const GraphNG: React.FC<GraphNGProps>;
