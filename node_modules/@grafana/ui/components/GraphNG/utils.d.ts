import { DataFrame, Field, Vector } from '@grafana/data';
import { AlignedFrameWithGapTest } from '../uPlot/types';
import { XYFieldMatchers } from './GraphNG';
export interface XYDimensionFields {
    x: Field;
    y: Field[];
}
export declare function mapDimesions(match: XYFieldMatchers, frame: DataFrame, frames?: DataFrame[]): XYDimensionFields;
/**
 * Returns a single DataFrame with:
 * - A shared time column
 * - only numeric fields
 *
 * @alpha
 */
export declare function alignDataFrames(frames: DataFrame[], fields?: XYFieldMatchers): AlignedFrameWithGapTest | null;
export declare function isLikelyAscendingVector(data: Vector): boolean;
