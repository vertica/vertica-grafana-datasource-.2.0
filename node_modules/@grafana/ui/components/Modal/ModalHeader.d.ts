import React from 'react';
import { IconName } from '../../types';
interface Props {
    title: string;
    icon?: IconName;
    iconTooltip?: string;
}
export declare const ModalHeader: React.FC<Props>;
export {};
