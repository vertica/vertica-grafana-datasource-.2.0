import { PureComponent } from 'react';
import { FieldSparkline, DataFrame, FieldConfig } from '@grafana/data';
import { GraphFieldConfig } from '../uPlot/config';
import { UPlotConfigBuilder } from '../uPlot/config/UPlotConfigBuilder';
import { Themeable } from '../../types';
export interface Props extends Themeable {
    width: number;
    height: number;
    config?: FieldConfig<GraphFieldConfig>;
    sparkline: FieldSparkline;
}
interface State {
    data: DataFrame;
    configBuilder: UPlotConfigBuilder;
}
export declare class Sparkline extends PureComponent<Props, State> {
    constructor(props: Props);
    componentDidUpdate(oldProps: Props): void;
    prepareData(props: Props): DataFrame;
    prepareConfig(data: DataFrame, props: Props): UPlotConfigBuilder;
    render(): JSX.Element;
}
export {};
